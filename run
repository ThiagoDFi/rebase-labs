#!/bin/bash

docker run \
  --rm \
  --name rebase-labs \
  --network rebase-lab \
  -w /app \
  -v $(pwd):/app \
  -p 3000:3000 \
  ruby \
  bash -c "gem install rack sinatra puma pg && ruby server.rb"
